import React from 'react';
import styled from 'styled-components';
import { Pagination } from 'rsuite';

const StyledAppPagination = styled(Pagination)`
  .rs-pagination-btn {
    font-size: 12px;
  }
`;

export default StyledAppPagination;
